<UserControl x:Class="WhiskyTest.View.PhraseSetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WhiskyTest.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <Label Content="{Binding Title}"></Label>
        <ListView ItemsSource="{Binding Phrases}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <DockPanel LastChildFill="False">
                        <TextBox DockPanel.Dock="Left" Text="{Binding Left}"></TextBox>
                        <Label>=</Label>
                        <TextBox DockPanel.Dock="Right" Text="{Binding Right}"></TextBox>
                    </DockPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackPanel>
</UserControl>
